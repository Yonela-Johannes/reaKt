import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    container: {
        display: 'flex',
        position: 'fixed',
        flexDirection: 'row',
        margin: 15,
        right: 50,
        top: 100,
        color: '#fff',
        width: '20px',
        alignItems: 'center',
    },
})

export default styles;